<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Our Social Media app</title>
    <!-- <link rel="stylesheet" href="../public/stylesheets/main.css"> -->
    <link rel="stylesheet" href="/stylesheets/main.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="wrapper m-0 p-0 box-border">
         
        <%- include('./partials/nav') %>

        <% if (messages.success) { %>
            <div id="successMessage" class="hidden bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative"
                role="alert">
                <strong class="font-bold">Success:</strong>
                <span class="block sm:inline" id="successText"><%= messages.success %></span>
            </div>
        <% } %>

        <% if (messages.error) { %>
            <div id="errorMessage" class="hidden bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
                <strong class="font-bold">Error:</strong>
                <span class="block sm:inline" id="errorText"><%= messages.error %></span>
            </div>
        <% } %>

        <div class="mx-4 sm:mx-16 lg:mx-56 mt-14 sm:mt-28 flex flex-col sm:flex-row justify-around gap-y-8 sm:gap-y-0 sm:gap-x-2">
            <div class="w-full sm:w-[60%] flex flex-col gap-7">
                <h1 class="text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-thin text-gray-800">Remember Writing?</h1>
                <p class="text-base text-gray-600">
                    Are you sick of short tweets and impersonal "shared" post that are reminiscent of the late 90's email forward?
                    We believe getting back to actually writing is the key to enjoying the internet again.
                </p>
            </div>

            <form class="w-full sm:w-[40%] flex flex-col gap-5" action="/user/signup" method="post">
                <div class="flex flex-col gap-1">
                    <label class="text-xl text-left" for="userName">Username</label>
                    <input required class="w-full h-10 p-2 border-2 rounded-sm" type="text" name="userName" id="userName"
                        placeholder="Pick a Username">
                    <p id="userMessage" class="hidden text-xs text-red-600"></p>
                </div>

                <div class="flex flex-col gap-1">
                    <label for="email">Email</label>
                    <input required class="w-full h-10 p-2 border-2 rounded-sm" type="email" name="email" id="email"
                        placeholder="you@example.com">
                    <p id="emailMessage" class="hidden text-xs text-red-600"></p>
                </div>

                <div class="flex flex-col gap-1">
                    <label for="password">Password</label>
                    <input required class="w-full h-10 p-2 border-2 rounded-sm" type="password" name="password" id="password"
                        placeholder="Create a Password">
                </div>

                <button type="submit" class="p-4 bg-green-500 flex justify-center items-center rounded-md text-white text-xl">
                    Sign up for OurApp
                </button>
            </form>
        </div>

         <%- include('./partials/footer') %>
    </div>
    
    <script src="/javascripts/home-guest.js"></script>
</body>
</html>