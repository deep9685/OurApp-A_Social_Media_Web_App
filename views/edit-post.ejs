<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Our Social Media app</title>
    <link rel="stylesheet" href="/stylesheets/main.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css"
    />
    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="wrapper m-0 p-0 box-border">
      <%- include('./partials/userNav') %>

      <div class="w-full h-auto flex justify-center">
        <form  action="/dashboard/edit-post/<%= post._id %>" method="post"  class="flex flex-col w-[50%] mt-16 gap-y-2">
            <!-- Hidden input to send the postId -->
        <!-- <input type="hidden" name="_method" value="PATCH"> -->
          <div class="flex flex-col w-full">
            <label
              class="font-light text-gray-700 text-xl"
              for="title"
              name="title"
              >Title</label
            >

            <input
              class="border border-gray-400 rounded-md py-2 p-1 text-xl"
              type="text"
              name="title"
              id="title"
              value="<%= post.title %>"
            />
          </div>

          <div class="flex flex-col w-full">
            <label class="font-light text-gray-700 text-xl" for="body"
              >Body</label
            >
            <textarea
              class="border border-gray-400 rounded-md h-64 p-1"
              name="body"
              id="body"
              
            ><%= post.content %></textarea>
          </div>

          <button
            class="px-2 h-10 bg-blue-600 text-white rounded-md w-fit flex justify-start items-center"
            type="submit"
          >
            Save changes
          </button>
        </form>
      </div>

      <%- include('./partials/footer') %>
    </div>
  </body>
</html>
